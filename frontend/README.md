# CelebrateLK Frontend

React + Vite frontend for the CelebrateLK MERN stack app.

## Run

```bash
npm install
npm run dev
```

Default URL: http://localhost:5173

The API URL is configured with `VITE_API_URL`. Copy `.env.example` to `.env` when you need to change it.
